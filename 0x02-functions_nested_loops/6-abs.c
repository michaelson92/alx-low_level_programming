#include "main.h"
/**
  *_abs: computes the absolute value of an integer
  *@x: the integer to compute
  *Return: absolute value x
  */
int _abs(int n)
{
	if (x >= 0)
		return (x);
	else
		return (-x);

}
